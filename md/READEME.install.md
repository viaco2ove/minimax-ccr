# windows
创建虚拟环境（推荐命名为 .venv）
`python -m venv .venv`
```
.venv\Scripts\activate
python python_tools/ofa_admin_api.py
```
# wsl
cd /mnt/d/Users/viaco/claude/PythonProject/doc
`python -m venv .venv_wsl`

```
source .venv_wsl/bin/activate
python python_tools/ofa_admin_api.py
```

# 安装库
pip install -r requirements.txt