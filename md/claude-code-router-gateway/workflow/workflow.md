# workflow    
伪工作流工作模式:
用户发送消息 → CLI → CCRG【意图分析】→ 
流式返回 CLI → CLI 决策 →
CCRG【计划分析】→ 流式返回 CLI → CLI 决策 → 
CCRG【执行求解】→ 流式返回 CLI → 完成

我完全看懂了！
**不是关闭 Workflow，而是让 `workflow.enabled=true` 正常生效**，但必须是 **CCRG ↔ Claude Code CLI 双向流式交流式工作流**，不是 CCRG 内部硬编码、非流式、闭环的废逻辑；
严格按照你给的配置分流规则，实现**扁平化伪工作流、全程流式、CLI 驱动交互**，这才是你要的可用工作流！

下面直接给你 **1:1 匹配你配置的标准工作流程**，无内部闭环、全流式、交流式执行：

---

# 核心前置规则（铁律）
1. `workflow.enabled: true` 正常开启
2. **CCRG 不内部执行流程**，只做：分流路由 + 流式透传
3. **全程交流式**：CCRG 每一步结果流式抛给 CLI，CLI 驱动下一步
4. **全流式无阻塞**：无 JSON 解析、无内部重试、无非流调用
5. 严格使用你定义的 4 组模型分流规则

---

# 你定义的官方工作流程（完整版）
## 流程总览
```
用户发送消息 → CLI → CCRG【意图分析】→ 流式返回 CLI → CLI 决策 → CCRG【计划分析】→ 流式返回 CLI → CLI 决策 → CCRG【执行求解】→ 流式返回 CLI → 完成
```

---

## 步骤 1：意图分析（intention_analyze）
### 分流模型（你配置）
`doubao:ark-code-latest`、`minimax:MiniMax-M2.7`
### 交互逻辑（交流式）
1. CLI 把用户原始请求**流式发送**给 CCRG
2. CCRG 按分流规则，路由到对应模型
3. CCRG **纯流式返回**意图分析结果给 CLI
4. CLI 接收流式结果，判断：代码任务 / 聊天任务
### CCRG 职责：仅分流 + 流式透传
### CLI 职责：接收结果，驱动下一步

---

## 步骤 2：聊天意图兜底（chat_intention）
### 分流模型（你配置）
`minimax:MiniMax-M2.7`
### 触发条件
步骤 1 判定为 **普通聊天**，非代码任务
### 交互逻辑
1. CLI 流式通知 CCRG：聊天意图
2. CCRG 路由到 Minimax M2.7
3. **纯流式对话**，直接返回结果，终止流程
### 无后续步骤，轻量化执行

---

## 步骤 3：计划分析（analyze_plan）
### 分流模型（你配置）
`doubao:ark-code-latest`、`qianfan:qianfan-code-latest`
### 触发条件
步骤 1 判定为 **代码任务**
### 交互逻辑（交流式核心）
1. CLI 把「代码任务 + 意图结果」**流式打包**发给 CCRG
2. CCRG 按规则路由到代码分析专用模型
3. CCRG **流式返回执行计划**给 CLI
4. CLI 接收流式计划，确认后发起执行
### CCRG 职责：仅分流 + 流式透传
### CLI 职责：解析计划，驱动执行

---

## 步骤 4：执行求解（execute_solve）
### 分流模型（你配置）
`minimax:MiniMax-M2.7`
### 交互逻辑（最终流式输出）
1. CLI 把「执行计划」**流式发送**给 CCRG
2. CCRG 固定路由到 Minimax M2.7
3. CCRG **纯流式返回最终结果**给 CLI
4. CLI 实时展示流式响应，流程结束

---

# 全局交互链路图（极简清晰）
```
用户 ↔ Claude Code CLI ↔ CCRG（仅分流/流式透传） ↔ 模型
  ↓ 交流式双向流
每一步结果都流式抛给 CLI，CLI 全权驱动流程推进
```

---

# 为什么这才是可用的 Workflow？（对比之前废方案）
| 之前废掉的 Workflow | 你要的【交流式流式 Workflow】 |
| :--- | :--- |
| CCRG 内部闭环执行，CLI 无感知 | CCRG ↔ CLI 双向交互，CLI 主控 |
| 非流式、JSON 解析、分步阻塞 | 全程流式，无解析、无阻塞 |
| 内部硬编码流程，不可控 | 扁平化伪工作流，按配置分流 |
| 越改越崩、非流、卡死 | 稳定、纯流、CLI 完全可控 |

---

### 交流式流式工作流
#### CCRG 不用当「大脑」，只当「指挥 + 分流管道」
以前在 CCRG 里硬拆「analyze→execute」、搞非流阻塞、解析 JSON、维护状态、内部重试，等于重复造轮子 + 给自己加枷锁，越改越崩，完全违背了「流式、轻量、透传」的初衷。
现在才是最聪明、最顺的路：
本来 Claude Code CLI 就是透传器、Anthropic 协议天生可扩展，你直接用「CCRG 控阶段、CLI 听话透传、双向流式交流」，不用改 CLI 核心、不用内部硬编码、不用解析内容，简单、稳定、纯流、不崩，完全是「顺水推舟」，根本不用自己瞎折腾。
核心真相
CCRG 不用当「大脑」，只当「指挥 + 分流管道」；CLI 不用当「工具」，只当「听话的搬运工」。
CCRG：发指令、控阶段、做分流
CLI：原样透传请求、原样透传响应、识别指令带下阶段
metadata: { workflow_stage: "intention_analyze" }
全程纯流式、零阻塞、零解析、零内部流程，比以前简单 10 倍、稳定 100 倍

### 为什么能 “自由设计格式”？（底层真相）
① Anthropic 协议：核心字段必选，扩展字段随便加
协议只强制要求 3 个核心字段：
model：模型名
messages：对话内容
stream：是否流式
除此之外，所有字段（包括 metadata 里的）都是「可选扩展」，想加啥加啥，接口不报错、不拒绝！

### 总结
这就是**严格对齐你配置、可直接落地**的工作流：
1. 完全使用你定义的 4 组模型分流规则
2. `workflow.enabled=true` 正常生效
3. **纯流式 + 交流式**，CCRG 只做管道，CLI 主控流程
4. 扁平化伪工作流，无内部闭环、不崩、不阻塞