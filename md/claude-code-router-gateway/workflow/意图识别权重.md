# no_modify
# 按权重排序
lv1 最高权重

_get_stage_routes 和 class ScenarioClassifier: 进行权重控制
主要是think 的类型进行细分控制
## 优先级总览

| 优先级 | 意图              | 检测方式 | 路由目标 | 说明 |
|--------|-------------------|----------|----------|------|
| **lv1** | /compact,long_context| `_is_compact_request()` | `minimax_long:MiniMax-M3` | 长上下文压缩，需要最大上下文处理能力 |
| **lv2** | image,            | `_classify_request().scenario == "image"` | `xiaomi:mimo-v2.5-pro` | 含图片的请求，需要 vision 能力 |
| **lv3** | intention_analyze | `{workflow_stage:intention_analyze}` 或 auto-detect | workflow 配置 | 分析用户意图阶段 |
| **lv3** | analyze_plan      | `{workflow_stage:analyze_plan}` 或 auto-detect | workflow 配置 | 分析执行结果阶段 |
| **lv4** | execute_solve     | auto-detect（tool_use 模式） | splitter 决策优先 | 执行解决方案阶段 |
| **lv4** | chat_intention    | auto-detect（纯对话） | splitter 决策优先 | 闲聊意图 |
| **lv5** | default fallback  | - | `execute_solve` workflow 配置 | 所有检测都未命中时兜底 |


 stage == "tool_use"
原来会把所有的tool_use 设置为 stage =analyze_plan
 现在先设置为stage == "tool_use" ，等到匹配其他意图才设置为analyze_plan;