# protocol 说明
务必搞清楚codeplan 和 chat 是完全不一样的接口！！！！ apikey 都是不通用的！！！！！
## codeplan_anthropic
走的是codeplan（agent工具）版的 anthropic 接口！！！！ 不是文本对话接口！！！！

## chat_openai
走的是“对话API” 的 接口！！！！ 不是codeplan（agent工具）接口！！！！


## mmx
走独立的mmx 命令方式。但是调试结果是不太稳定
但是可以用于图形理解。(比 minimax（agent工具）好，但过比glm5.1等模型差)


## default_params.max_tokens
输入总 token + max_tokens ≤ 模型上下文窗口上限
capabilities.capabilities + default_params.max_tokens 要小于模型上下文窗口上限

# 一些终重要参数说明
max_context 的效果：控制消息截断阈值。

  当预估 token 数 > max_context * 80% 时，会自动截断消息保留最近的对话，使总 token 控制在安全范围内。

  - minimax 配置 max_context: 32000 → 截断阈值 = 25600 tokens
  - qianfan 配置 max_context: 128000 → 截断阈值 = 102400 tokens

  所以不同 provider 的 max_context 决定了它们能"吃下"多大的上下文，越大的模型截断越宽松。你的 minimax 配置比较保守（32k），qianfan 更大（128k）。

# 安全
## 样例文件不要记录api_key！
[.gateway.exsample.json](gateway_exsample/.gateway.exsample.json)
[.gateway.exsample.v2.json](gateway_exsample/.gateway.exsample.v2.json)
正侧替换
"api_key": ".* -》"api_key": "xxxx",


# "local_model"
请自行设置足够多的RAM，或者使用足够多 swap文件（虚拟内存）。
mlock: false 即可让系统自动换出到虚拟内存
use_mmap内存映射文件，不需要全部加载到 RAM