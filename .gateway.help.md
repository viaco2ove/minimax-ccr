# CCR 网关配置文件使用说明
重点简略说明:
[.gateway.md](.gateway.md)
## 概述

本系统使用两个核心配置文件：

| 文件 | 用途 |
|------|------|
| `.gateway.json` | 网关主配置（服务器、提供商、路由、配额、工作流） |
| `keywords.json` | 关键词分类库（用于意图分析、任务路由） |

---
## 错误信息说明
[md/error.md](md/error.md)

## 一、keywords.json - 关键词分类库

### 1.1 workflow_intent（工作流意图分类）

用于分析用户输入的意图类型，决定走哪个工作流阶段。

| 字段 | 说明 | 示例关键词 |
|------|------|-----------|
| `chat_intention` | 闲聊/问候意图 | 你好、在吗、谢谢、hello |
| `intention_analyze` | 意图分析请求 | 我想、请问、如何、需要 |
| `problem_analyze` | 问题诊断请求 | 分析、排查、为什么、报错 |
| `solution_plan` | 方案设计请求 | 方案、解决、步骤、计划 |
| `execute_solve` | 执行解决请求 | 执行、修改、生成、修复 |

**使用场景**：当用户说"帮我分析为什么接口这么慢"时，系统识别到`problem_analyze`关键词，会走问题分析工作流。

### 1.2 task_routing（任务路由分类）

用于根据任务类型选择合适的提供商。

| 字段 | 任务类型 | 适用场景 | 示例关键词 |
|------|----------|----------|-----------|
| `cheap_tasks` | 轻量级任务 | 读取、查看、搜索 | cat、ls、grep、git status |
| `standard_tasks` | 标准任务 | 编辑、写入 | edit、write、新建文件 |
| `complex_tasks` | 复杂任务 | agent、规划 | agent、智能体、架构 |
| `long_context` | 长上下文 | 大文件/全文 | 长文本、完整项目 |
| `web_search` | 网络搜索 | 查资料 | 搜索、查找、最新 |
| `image` | 图片处理 | 看图、OCR | 截图、看图、vision |
| `compact` | 简洁模式 | 简短回复 | /compact、简洁 |

### 1.3 expert_keywords（专家级关键词）

用于标识需要专家级能力的复杂任务。

```json
"architecture_refactor": ["架构", "重构", "设计模式", "模块化", ...]
```

---

## 二、.gateway.json - 网关主配置

### 2.1 server（服务器配置）

```json
"server": {
  "host": "127.0.0.1",      // 监听地址
  "port": 3428,             // 监听端口
  "timeout_ms": 600000,      // 请求超时（毫秒）
  "log_level": "debug",     // 日志级别
  "log_file": "logs/ccrg.log", // 日志文件路径
  "proxy_url": "",          // 代理URL（可选）
  "api_key": "",            // 服务API密钥（可选）
  "claude_path": ""         // Claude CLI路径（可选）
}
```

### 2.2 providers（AI提供商配置）

| 字段 | 说明 | 示例 |
|------|------|------|
| `api_base_url` | API地址 | `https://api.minimaxi.com/anthropic` |
| `api_key` | API密钥 | `sk-cp-xxx` |
| `protocol` | 协议类型 | `codeplan_anthropic` / `chat_openai` |
| `models` | 可用模型列表 | `["MiniMax-M2.7"]` |
| `capabilities` | 模型能力 | tool_use、streaming、thinking、vision |
| `cost_tier` | 成本等级 | `cheap` / `premium` |
| `timeout_ms` | 请求超时 | `120000` |
| `per_request_delay_ms` | 请求间隔 | `500`（用于限流） |

**当前配置的提供商**：

| 提供商 | 用途 | 成本 |
|--------|------|------|
| minimax | 主提供商 | cheap |
| qianfan | 备用 | cheap |
| doubao | 备用/Thinking模式 | cheap |
| mmx | 本地测试 | cheap |
| deepseek | 高端任务 | premium |

### 2.3 routing（路由配置）

#### 2.3.1 default（默认路由）

```json
"default": "minimax:MiniMax-M2.7"
```

格式：`provider:model`，找不到匹配时使用此路由。

#### 2.3.2 scenarios（场景路由）

| 场景 | 主路由 | 备用 | 说明 |
|------|--------|------|------|
| `background` | minimax | doubao | 后台任务 |
| `think` | doubao | doubao/minimax | 思考模式 |
| `long_context` | doubao | doubao/minimax | 长上下文(>100K tokens) |
| `web_search` | minimax | doubao/minimax | 网络搜索 |
| `image` | mmx | minimax/doubao | 图片处理 |
| `compact` | minimax | doubao | 简洁模式 |

#### 2.3.3 tool_routing（工具路由）

根据调用的工具类型选择提供商：

| 任务类型 | 匹配工具 | 路由 |
|----------|----------|------|
| `cheap_tasks` | Read, Glob, Grep, Bash(git status)等 | minimax |
| `standard_tasks` | Edit, Write, NotebookEdit | minimax |
| `complex_tasks` | Agent, TaskCreate, EnterPlanMode | doubao |

#### 2.3.4 keyword_routing（关键词路由）

根据用户消息中的关键词选择路由：

```json
{
  "keywords": ["架构", "重构", "优化", ...],
  "match_mode": "any",
  "scope": "user_message",
  "route": "doubao:doubao-seed-2.0-code",
  "fallback": ["minimax:MiniMax-M2.7"]
}
```

**match_mode选项**：
- `any`：包含任一关键词即匹配
- `all`：包含所有关键词才匹配

**scope选项**：
- `user_message`：仅匹配用户消息
- `all`：匹配所有消息

#### 2.3.5 priority（路由优先级）

```json
"priority": ["scenario", "tool_routing", "keyword_routing", "default"]
```

按顺序检查，越前面优先级越高。

### 2.4 quota（配额管理）

用于追踪和控制API使用量：

```json
"quota": {
  "minimax_codeplan": {
    "enabled": true,                    // 是否启用
    "provider": "minimax",              // 跟踪的提供商
    "track_usage": true,                // 是否追踪使用量
    "fallback_on_exhaust": "minimax:MiniMax-M2.7",  // 配额用尽后的备用
    "warning_threshold": 0.2            // 告警阈值（20%）
  }
}
```

### 2.5 workflow（工作流配置）

用于意图分析时选择不同阶段的提供商：

```json
"workflow": {
  "enabled": true,
  "intention_analyze": ["minimax:MiniMax-M2.7"],  // 意图分析阶段
  "chat_intention": ["minimax:MiniMax-M2.7"],      // 闲聊意图阶段
  "analyze_plan": ["doubao:doubao-seed-2.0-code"], // 分析计划阶段
  "execute_solve": ["minimax:MiniMax-M2.7"]        // 执行解决阶段
}
```

---

## 三、配置示例

### 3.1 添加新提供商

```json
"providers": {
  "openai": {
    "api_base_url": "https://api.openai.com/v1",
    "api_key": "sk-xxx",
    "protocol": "chat_openai",
    "models": ["gpt-4"],
    "capabilities": {
      "tool_use": true,
      "streaming": true,
      "thinking": false,
      "vision": true,
      "max_context": 128000
    },
    "cost_tier": "premium",
    "timeout_ms": 120000
  }
}
```

### 3.2 添加场景路由

```json
"scenarios": {
  "code_review": {
    "route": "deepseek:deepseek-chat",
    "fallback": ["minimax:MiniMax-M2.7"]
  }
}
```

### 3.3 添加工具路由规则

```json
"tool_routing": {
  "web_search": {
    "match": ["WebSearch", "WebFetch"],
    "match_mode": "any",
    "route": "deepseek:deepseek-chat",
    "fallback": ["minimax:MiniMax-M2.7"]
  }
}
```

### 3.4 添加关键词路由

```json
{
  "keywords": ["安全", "漏洞", "渗透测试"],
  "match_mode": "any",
  "scope": "user_message",
  "route": "deepseek:deepseek-chat",
  "fallback": ["doubao:doubao-seed-2.0-code"]
}
```

---

## 四、路由决策流程

```
用户请求
    │
    ▼
┌─────────────────┐
│ 场景检查(scenario) │
└────────┬────────┘
         │ 匹配?
    ┌────┴────┐
    │YES │NO  │
    ▼    └────┐
工具路由    ▼
    │ 匹配?
    ├────┬────┐
    │YES │NO  │
    ▼    └────┐
关键词路由  ▼
    │ 匹配?
    ├────┬────┐
    │YES │NO  │
    ▼    └────┐
默认路由    ▼
         发送请求
```

---

## 五、最佳实践

1. **成本优化**：轻量任务用 `cheap` 提供商，复杂任务用 `premium`
2. **可靠性**：始终配置 `fallback` 防止主提供商故障
3. **限流**：使用 `per_request_delay_ms` 避免触发限流
4. **监控**：启用 `quota.track_usage` 追踪使用量
5. **关键词覆盖**：定期更新 `keywords.json` 提升路由准确率